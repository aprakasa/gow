## gow site ssl

Enable SSL with Let's Encrypt

```
gow site ssl <domain> [flags]
```

### Options

```
      --dns string     DNS provider for DNS-01 (cloudflare)
      --email string   Let's Encrypt registration email
  -h, --help           help for ssl
      --hsts           Emit Strict-Transport-Security header
      --staging        Use Let's Encrypt staging server
      --wildcard       Issue wildcard cert (*.domain); requires --dns
```

### Options inherited from parent commands

```
      --conf-dir string      OLS config base directory (default "/usr/local/lsws/conf")
      --policy-file string   Allocator policy override (default "/etc/gow/policy.yaml")
      --state-file string    Site registry file (default "/etc/gow/state.json")
      --web-root string      Base directory for site document roots (default "/var/www")
```

### SEE ALSO

* [gow site](gow_site.md)	 - Manage WordPress sites

###### Auto generated by spf13/cobra on 23-Apr-2026
